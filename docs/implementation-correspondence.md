# Implementation Correspondence

This document records the intended correspondence between the Quint model and
the current Zebra Crosslink/Tenderlink prototype.

The model is intentionally abstract. It should still stay close enough to the
implementation that each rule has a clear code counterpart.

## Tenderlink Round Recovery

| Quint rule | Implementation counterpart | Notes |
| --- | --- | --- |
| `UponValuePrevoteQuorum` | Tenderlink `bft_update` handling of locally received `2f + 1` prevotes for the proposal id | The implementation freshly validates the proposal before precommitting a value. |
| `UponStreamChangePrecommitNil` | Tenderlink stale validation path that broadcasts `Precommit(ValueId::NIL)` | This is the Crosslink-specific moving-stream hook. |
| `StaticProposalValidity` | Crosslink proposal/block validation before freshness checks | Abstract split for structural validity, PoW-chain validity, and finality-candidate validity. |
| `ValidRoundJustified` / `AcceptableProposalFor` / `CorrectProposalValidRoundSound` | Proposal receive checks for `validRound`/POLRound evidence | A non-`-1` valid round must be backed by a prevote quorum for the proposal value before it can unlock an older value lock. |
| Heighted `ValidRoundJustified` / `AcceptableProposalFor` | Per-BFT-height proposal receive checks for `validRound`/POLRound evidence | The heighted slice scopes the POL evidence to the same BFT height as the proposal and rejects unjustified cross-value unlocks. |
| `CrosslinkTenderlinkProposalPolEvidence.qnt` / `ProposalPolEvidenceCanonical` | Proposal receive checks after decoding legacy Tenderlink proposal chunks and vote packets | Bridges the abstract valid-round rule to production-shaped bytes: a non-nil `validRound` proposal chunk must be paired with a canonical prevote packet whose height, round, value id, and packet-derived voting power justify the proposal. |
| `StartNextRoundAfterPrecommitQuorum` | Tenderlink local precommit-quorum processing and round start | In the resampling branch, a locally delivered nil quorum clears same-round state before moving on. |
| `AdvanceAfterPrecommitQuorum` in `CrosslinkHeightedRound.qnt` | Height-indexed Tenderlink round advancement | The heighted slice scopes nil-precommit clearing to the validator's active BFT height and current round; a mixed precommit quorum does not clear the heighted lock/cache state. |
| `CrosslinkMixedWaitProgressContract.qnt` / `EventuallyNilCertUnlocksMixedWait` | Tenderlink precommit wait behavior under mixed precommit delivery | This is a TLC-friendly temporal contract rather than a direct code counterpart. It checks that a mixed precommit quorum records waiting without clearing same-round lock/cache state, then only an actual nil-precommit certificate unlocks the round and permits resampling; older Tendermint locks remain preserved. |
| `CrosslinkHeightedProgressProjectionContract.qnt` / `EventuallyHeightedProgressFinalizesTwoHeights` | Height-indexed mixed-precommit wait, nil-precommit recovery, and finality application | This is a TLC-friendly projection rather than a direct code counterpart. It checks that a mixed precommit quorum does not unlock height 1 or touch height 2, then nil-precommit recovery clears only height 1, height 2 remains pristine until height 1 decides, and finality consumes the two decided heights in order. |
| `CrosslinkHeightedAuthenticatedProgressProjectionContract.qnt` / `EventuallyHeightedAuthenticatedProgressFinalizesTwoHeights` | Height-indexed nil-precommit recovery, authenticated observer evidence, and finality application | This is a TLC-friendly projection rather than a direct code counterpart. It reuses the two-height mixed-wait/nil-certificate path, but replaces bare finality with authenticated finality: a height is finalized only after observer-local precommit evidence and fat-pointer signatures from the correct quorum are present for that same height and round. |
| `CrosslinkRotatingAuthenticatedProgressProjectionContract.qnt` / `EventuallyRotatingAuthenticatedProgressFinalizesTwoHeights` | Height-indexed nil-precommit recovery, validator-set rotation, authenticated observer evidence, and finality application | This is a TLC-friendly projection rather than a direct code counterpart. It checks that height 1 recovers and decides under the old validator set, installs the height-2 validator set, rejects removed/new validators at the wrong evidence height, and finalizes both heights only after authenticated evidence from signers authorized by that height's set. |
| `ApplyLateNilPrecommitCertificate` | Tenderlink late locally delivered nil-certificate recovery path | Late certs clear abandoned-round state without rewinding the validator. |
| `MixedPrecommitQuorumWithoutNilCert` | Negative guard test in Tenderlink | Mixed precommits can end waiting, but cannot unlock. |
| `TimeoutProposePrevoteNil` | Propose-timeout handler | Local timeout emits nil prevote without touching lock or valid state. |
| `TimeoutPrevotePrecommitNil` | Prevote-timeout handler | Local timeout emits nil precommit; a quorum of these can become nil-certificate evidence. |
| `TimeoutPrecommitStartNextRound` | Precommit-timeout handler | Advances to the next round while preserving state unless a separate nil-precommit certificate is processed. |
| Heighted `Timeout*` rules | Per-BFT-height timeout handling | The heighted round-machine slice scopes timeouts to the validator's active BFT height and checks that timeout-only round advancement does not clear heighted locks. |
| `CrosslinkTimeoutProgressContract.qnt` / `EventuallyTimeoutRecoveryDecides` | Timeout scheduler and nil-certificate recovery contract | This is a TLC-friendly temporal contract rather than a direct code counterpart. It checks that ordinary timeouts do not clear older Tendermint locks, nil-precommit certificates clear only same-round recovery state, and a later stable proposal must be justified before deciding across an older lock. |
| `DeliverProposal` / `DeliverPrevote` / `DeliverPrecommit` | P2P receive path plus per-peer/per-validator message cache | Delivery is modeled separately from broadcast. A receiver-local quorum only exists after the signed messages have been delivered into that validator's local view, and active round transitions now consume that local view. |
| `CrosslinkDeliveryFairnessContract.qnt` / `EventuallyLocalDecision` | Network delivery fairness assumption around the receive path | This is a TLC-friendly temporal contract rather than a direct code counterpart. It checks that broadcast messages do not create local quorum evidence by themselves, that local prevote quorum gates precommit broadcast, and that fair delivery eventually gives a correct decider a local precommit quorum. |
| `VotingPowerOf` / `QuorumVotingPower` | Validator-set voting power and quorum threshold | The current executable examples include both equal-weight and non-uniform-power instances. |
| `CrosslinkValidatorSetChange.qnt` / `DecideAt` | Validator-set update at BFT-height boundaries | A decided value installs the next validator set; removed validators cannot contribute to quorum at the next BFT height. |

## Crosslink Finality

| Quint rule | Implementation counterpart | Notes |
| --- | --- | --- |
| `ValidFinalityCandidate` | Crosslink BFT block/finality candidate validation | Should check branch extension, sigma/tail confirmation, and declared candidate height. |
| `CrosslinkHeadSigmaSampling.qnt` / `HeadSigmaCandidate` | `zebra-crosslink/src/lib.rs::propose_new_bft_block`; `zebra-crosslink/src/chain.rs::BftBlock::try_from` | The model derives the proposal stream as the ancestor at `headHeight - sigma`. The prototype computes `finality_candidate_height = tip_height - bc_confirmation_depth_sigma`, fetches that candidate header, then builds a `BftBlock`; `BftBlock::try_from` currently enforces that the proposal carries exactly `bc_confirmation_depth_sigma` headers. |
| `CrosslinkStreamChurnRisk.qnt` / `StreamChurnRiskNumerator` | Crosslink parameter selection for sigma, round duration, and validator-set sizing | This is an analysis layer rather than a direct code counterpart. It records the liveness argument that larger globally distributed validator sets increase the prevote-to-precommit churn window through GST, sigma mitigates only long-reorg-tail churn, and nil-precommit resampling converts finite stream churn into round increments until the stream stabilizes. |
| `CrosslinkBaselineChurnProgressContract.qnt` / `EventuallyResamplingOutrunsBaselineHalt` | Baseline sticky proposal/lock carry behavior versus nil-precommit resampling recovery | This is a TLC-friendly temporal contract rather than a direct code counterpart. It isolates the failure mode where baseline Crosslink carries a stale cached stream sample across a round change, then checks that nil-precommit resampling can burn nil-certified rounds and decide after a stable stream value appears. |
| `CrosslinkValidatorScaleLivenessEnvelope.qnt` / `ResamplingStrictlyExtendsValidatorEnvelope` | Crosslink validator-set sizing and round-time parameter analysis | This is a bounded analysis layer rather than a direct code counterpart. It checks the qualitative sizing claim: a larger globally distributed validator set can exceed the baseline halt-risk budget while remaining within a nil-resampling round-burn budget, and increasing sigma does not reduce ordinary PoW head-arrival exposure during the prevote/precommit window. |
| `CrosslinkValidatorScaleProgressContract.qnt` / `EventuallyResamplingDecision` | Validator-scale liveness contract for nil-precommit resampling | This is a TLC-friendly temporal contract rather than a direct code counterpart. It checks the stressed scale case where baseline Crosslink halts after the prevote/precommit stream-change hazard, while nil-precommit resampling burns the bounded churn rounds and then reaches a stable decision. |
| `CrosslinkValidatorScaleFinalityProgressContract.qnt` / `EventuallyValidatorScaleFinalized` | Validator-scale liveness plus Crosslink finality contract | This is a TLC-friendly temporal contract rather than a direct code counterpart. It reuses the validator-scale progress machine, then checks that a fair finality applicator advances the finalized prefix to the stable candidate while preserving prefix linearity and rejecting the competing fork candidate. |
| `CrosslinkPowStochasticAssumptions.qnt` / `UsesZebraPostBlossomSpacing` / `NormalArrivalUsesPoissonUnionBound` | `zebra-chain/src/parameters/network_upgrade.rs::POST_BLOSSOM_POW_TARGET_SPACING`; Crosslink sigma/GST/reorg-tail parameter analysis | This executable assumption profile pins the normal PoW-arrival denominator to Zebra's 75-second post-Blossom target spacing, models the prevote-to-precommit exposure window as the one-block Poisson/union-bound numerator, and constrains the long-reorg-tail numerator to a geometric sigma-decay profile. The numeric tail and propagation-risk inputs are still assumptions to replace with measured distributions. |
| `CrosslinkPowFixtureMeasuredDistribution.qnt` / `HeaderTimeOffsetMatchesZcashHeaderLayout` / `CheckedInFixtureIntervalDistributionPinned` | `crosslink-test-data/test_pow_block_*.bin`; `zebra-chain` serialized block header layout; `fixtures/production-bft-block-vectors.json`; `spec/CrosslinkProductionFixtureVectorsGenerated.qnt`; `scripts/extract-bft-block-vectors.mjs` | Adds a measured fixture-data bridge below the stochastic assumption profile. The extractor now records the raw PoW fixture header-time field at byte offset 100, the checked-in height/time span, interval buckets, and average interval ratio. The current fixture stream has 23 checked intervals over 27,000 seconds, with 18 zero-second intervals and 5 intervals above the 75-second target, so the spec deliberately records these as deterministic serialization fixtures rather than mainnet stochastic calibration data. |
| `CrosslinkPowForkSchedule.qnt` / `RoundRollbackDepth` / `SampleSurvivesRollback` | Future PoW best-tip observation schedule for Crosslink stream sampling | Derives rollback depth from consecutive observed best-tip snapshots and checks sigma against the derived fork switch rather than relying on a hand-labelled reorg input. No current prototype counterpart exists yet. |
| `CrosslinkPowBranchCompetition.qnt` / `IsBestTipAt` / `RoundRollbackDepth` | Future PoW branch-competition and adversarial release model for Crosslink stream sampling | Adds the work-selection layer under the best-tip schedule: a tip must be published before it can win, published tips compete by honest plus adversarial work with deterministic tie-breaking, and an adversarial release can derive the rollback signal consumed by nil-precommit resampling or dynamic sigma. No current prototype counterpart exists yet. |
| `CrosslinkPowReorgStress.qnt` / `ProcessRound` | Crosslink vote-status handling when `proposal_status_against_current_stream` reports stale between prevote and precommit | This bounded stress slice records the concrete behavior expected from the nil-precommit variant: long reorgs and same-branch PoW head arrivals can both change the sampled `head - sigma` candidate within one round, so correct validators abort with nil precommits and only decide once prevote and precommit see the same candidate. |
| `CrosslinkBftHeights.qnt` / `ApplyScheduledDecision` / `ApplyDecisionAt` | Future BFT-height finality applicator | Models consecutive BFT decisions updating the Crosslink finality cursor, while rejecting skipped consensus heights and fork decisions after a prefix is final. The richer heighted finality specs now cover this inside a round-machine composition, but this remains a compact scheduled-height sanity harness. |
| `CrosslinkDynamicSigma.qnt` / `NextSigmaForHeight` / `CommitHeightWithTelemetry` | Future dynamic-sigma Crosslink variant; consensus params for `bc_confirmation_depth_sigma` by BFT height | This is a third Crosslink variant rather than a local tuning rule. It models sigma as a consensus-visible per-height schedule: same-height nil-precommit round burns do not change sigma, height transitions compute the next sigma deterministically from committed failure telemetry, validator/network coverage evidence, and the percentage of PoW hash power participating in Crosslink. Same-branch block-arrival failures are not sigma-relevant; long-reorg or ambiguous low-coverage failures can raise future sigma; low participating hash power contributes to sigma-relevant pressure, prevents relaxation below the target, and directly raises sigma below a critical floor; stable well-covered, high-participation windows lower sigma slowly. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaCalibration.qnt` / `CalibratedSigmaFloorForObservation` / `CalibrationSafety` | Future measured-window calibration harness for dynamic sigma | Checks the simpler pre-production calibration contract: hash participation, round-failure rate, block-interval variance, and observed reorg depth are weighted into risk scores and must classify bounded measured windows into expected sigma floors. This sits below the production telemetry contract. |
| `CrosslinkDynamicSigmaTelemetry.qnt` / `TelemetrySigmaFloorForObservation` / `EconomicTargetSatisfiedAtSigma` | Future production telemetry inputs for dynamic sigma | Adds a production-shaped calibration contract around committed telemetry: participating work must be measured against total observed PoW work, coverage and round-failure estimates must upper-bound raw samples, rollback risk must be monotone across the sigma ladder, and selected sigma must satisfy rollback-risk plus expected-loss budgets whenever the configured ladder can satisfy them. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaForkSchedule.qnt` / `DerivedControllerSigma` / `DerivedReorgDepth` | Future dynamic-sigma controller fed by observed PoW best-tip transitions | Composes the controller with the fork-schedule model so rollback depth is derived from consecutive best-tip snapshots before sigma is raised. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaBranchCompetition.qnt` / `BranchCompetitionControllerSigma` / `DerivedReorgDepth` | Future dynamic-sigma controller fed by generated PoW branch competition | Composes the controller with published-tip honest/adversarial work competition, so an adversarial branch release can become the best tip and raise sigma through the derived rollback-depth path. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaResampling.qnt` / `AdvanceDerivedForkSignal` / `DynamicResamplingSafety` | Future composition of dynamic sigma with nil-precommit resampling | Checks that a generated fork signal can raise sigma before the abandoned Tenderlink round advances, while preserving the nil-precommit resampling path to a fresh decision. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaFinality.qnt` / `FullFinalizeDecision` / `FullComposedSafety` | Future composition of dynamic sigma, resampling, generated branch competition, and Crosslink finality | Uses live dynamic sigma as the finality tail-confirmation depth, rejects underconfirmed candidates and skipped BFT finality heights, and checks that low participation can delay finality. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaConsensusParams.qnt` / `CommitHeightWithConsensusParams` / `ConsensusParamSafety` | Future consensus-parameter encoding for dynamic sigma | Adds the parameter payload boundary around the dynamic controller. A height transition must install canonical `bc_confirmation_depth_sigma` bytes whose decoded sigma matches `NextSigmaForHeight`, including the controller's Crosslink-participating hash-power input; nil-precommit round burns cannot rewrite the active height's params; malformed, stale, or out-of-range next-height sigma bytes are rejected. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaConsensusParamFormat.qnt` / `CommitHeightWithProductionConsensusParamWire` / `ProductionConsensusParamFormatSafety` | Future production byte layout for dynamic-sigma consensus params | Pins a compact production-shaped envelope for `bc_confirmation_depth_sigma`: one-byte key tag, u32 activation height, u32 telemetry source height, and u16 sigma value, with exact little-endian hex vectors for accepted and malformed envelopes. Accepted bytes are decoded into the abstract consensus-param wire before the controller transition; wrong-key, stale-activation, trailing-byte, and out-of-range-sigma envelopes are rejected. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaConsensusParamTransport.qnt` / `CommitHeightFromGossipedConsensusParam` / `ParamTransportSafety` | Future signed gossip and node config application for dynamic-sigma consensus params | Requires quorum-signed canonical production param bytes on the Crosslink consensus gossip topic before a node config follows a committed next-height `bc_confirmation_depth_sigma`; the signed payload is the format model's exact hex string for the production wire, and accepted production wires are decoded through the format model and stored beside the decoded abstract config. Wrong-topic, wrong-kind, wrong-byte, wrong-signature, malformed production envelopes, no-quorum, quorum-signed stale activation, and quorum-signed-but-nondeterministic sigma updates are rejected. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaHeadSampling.qnt` / `DynamicHeadSigmaAt` / `SigmaForHeight` | Future proposal-stream construction for dynamic-sigma Crosslink | Bridges dynamic sigma into the proposal stream: validators sample the active height's candidate as `head - sigma(h)`, nil-precommit round burns do not change same-height sigma or already sampled candidates, and committed telemetry only changes candidate depth for future BFT heights. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaHeightedRound.qnt` / `DynamicHeightedHeadSigmaSafety` | Future dynamic-sigma Tenderlink round handling | Carries the dynamic `head - sigma(h)` stream into the height-indexed round machine. Correct fresh proposals and value precommits must use the active height's dynamic candidate, nil-precommit resampling keeps the same height's sigma fixed, and after a local BFT-height transition the next proposal uses telemetry-updated sigma. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaHeightedFinality.qnt` / `FinalizeNextDynamicDecided` / `DynamicHeightedFinalitySafety` | Future dynamic-sigma Crosslink finality validation | Composes dynamic `head - sigma(h)` rounds with the finality cursor. A finalized BFT height must have a local heighted decision, must carry that height's dynamic stream candidate, and must be tail-confirmed by the active height's sigma rather than a fixed confirmation depth. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaHeightedAuthenticatedEvidence.qnt` / `DynamicAuthenticatedEvidenceSafety` | Future dynamic-sigma authenticated evidence boundary | Wraps heighted authenticated evidence so signed value precommits, observed precommits, fat-pointer signatures, and observed fat pointers are constrained to the active height/round's `head - sigma(h)` candidate. No current prototype counterpart exists yet. |
| `CrosslinkDynamicSigmaAuthenticatedFinality.qnt` / `FinalizeNextDynamicAuthenticated` / `AuthenticatedDynamicFinalitySafety` | Future dynamic-sigma finality plus authenticated evidence | Requires dynamic finality advancement to be backed by observer-local authenticated precommit evidence and authenticated fat-pointer signatures for the same height, round, and dynamic candidate. No current prototype counterpart exists yet. |
| `CrosslinkFinalityProgressContract.qnt` / `EventuallyFinalized` | Finality application after a stable Tenderlink decision | This is a TLC-friendly temporal contract rather than a direct code counterpart. It checks the handoff from the stable-decision phase to advancing the Crosslink finality cursor under a fair finality applicator. |
| `CrosslinkComposedProgressContract.qnt` / `EventuallyFinalizesStableDecision` | Nil-resampling round recovery followed by finality application | This self-contained temporal contract keeps the nil-precommit burned-round behavior, stable proposal/vote/precommit delivery, finality-candidate validation, and finalized-prefix linearity in one finite state graph that TLC can exhaustively check. |
| `CrosslinkComposedImportedProgressBridge.qnt` / `EventuallyImportedPredicateBridgeFinalizes` | Imported composed predicate bridge for nil-resampling and finality | This TLC-friendly temporal bridge imports `CrosslinkComposed.qnt` quorum-power, lock-scope, finality, and fork-prefix predicates into a scalar progress graph, checking that the nil certificate, same-round recovery clear, older-lock preservation, stable prevote quorum, stable precommit quorum, and finality step are all gated through imported predicates. It also rejects phase skips across resampling, proposal delivery, prevote delivery, precommit delivery, decision, and finality while the full imported round-machine action graph remains an open temporal-proof target. |
| `CrosslinkHeightedHeadSigmaRound.qnt` / `HeadSigmaSafety` | Heighted Tenderlink proposal, prevote, and precommit handling over the Crosslink proposal stream | The model ties each BFT-height/round `Stream(h, r)` value to the current `head - sigma` candidate, then checks that fresh correct proposals and value precommits use that candidate while nil-precommit certificates clear cached same-round state before the next round resamples. |
| `CrosslinkBftBlockShape.qnt` / `FindHeadersAfter` / `StatelessBftBlockShapeValid` | `zebra-state::Request::FindBlockHeaders`; `zebra-crosslink/src/lib.rs::propose_new_bft_block`; `zebra-crosslink/src/chain.rs::BftBlock::try_from`; `validate_bft_block_from_malachite_already_locked` | Zebra's Find protocol returns headers after the known intersection. With `known_blocks = [candidate_hash]`, the modeled proposal carries the sigma descendant headers after the declared `head - sigma` candidate; the model records that a validator check treating `headers.first()` as the candidate rejects that locally produced shape. The same slice models the desired stateless `BftBlock::try_from` guards and records current gaps: the prototype constructor currently only enforces header count, and `ZcashDeserialize` constructs `BftBlock` directly under a 2048-header envelope cap instead of delegating to `try_from`. |
| `CrosslinkBftBlockValidationGap.qnt` / `PrototypeBftBlockTryFromAccepts` / `RepairedBftBlockTryFromAccepts` / `RepairedBftBlockDeserializeAccepts` | `zebra-crosslink/src/chain.rs::BftBlock::try_from`; `zebra-crosslink/src/chain.rs::BftBlock::zcash_deserialize` | This standalone slice isolates the current constructor and deserializer boundaries: intended acceptance requires sigma header count, known versions, consecutive order, and valid PoW solutions; prototype constructor acceptance only requires sigma header count; prototype deserializer acceptance only requires the counted-header envelope to be within the 2048-header cap. The witnesses pin bad-version, bad-order, bad-PoW, and wrong-count over-acceptance as implementation gaps, then pin repaired constructor and deserializer targets that match intended acceptance exactly and reject the known malformed classes. |
| `CrosslinkBftBlockProductionVectors.qnt` / `BftBlockWireByteLen` / `HeadersStartOffset` | `zebra-crosslink/src/chain.rs::BftBlock::{zcash_serialize, zcash_deserialize}`; `zebra-crosslink/src/chain.rs::BftBlockAndFatPointerToIt::{zcash_serialize, zcash_deserialize}`; `zebra-crosslink/src/lib.rs::FatPointerToBftBlock2::{zcash_serialize, zcash_deserialize}`; `zebra-chain/src/block/serialize.rs::Header::zcash_serialize`; `crosslink-test-data/test_pos_block_*.bin`; `crosslink-test-data/test_pow_block_*.bin`; `fixtures/production-bft-block-vectors.json`; `spec/CrosslinkProductionFixtureVectorsGenerated.qnt`; `scripts/extract-bft-block-vectors.mjs` | Pins the production BFT-block wire prefix: u32 version, u32 BFT height, counted previous-block fat pointer, u32 finalization-candidate height, u32 header count, then contiguous serialized PoW headers. The generated manifest and generated Quint module now record both current fixture classes, including serialized BFT-block version/height, finalization-candidate height, and finalization/header-count/header-start offsets: `test_pos_block_5.bin` has version 0, BFT height 1, finalization candidate 0, a 62-byte header start, zero previous fat-pointer signatures, a 4523-byte BFT block, and a 4665-byte `BftBlockAndFatPointerToIt`; later POS/BFT envelopes have version 0, serialized BFT height 2, finalization candidate 0, a 158-byte header start, one previous fat-pointer signature, a 4619-byte BFT block, and a 4761-byte envelope. Raw `test_pow_block_*.bin` samples cover 24 checked-in blocks across heights 0..29 with version-4 1487-byte headers, 105/106-byte bodies, and generated header-time measurements. The generated module also pins the checked-in raw-PoW interval distribution, the complete checked-in BFT-height sequence, the zero/one previous-fat-pointer fixture counts, the total embedded header/raw-match counts, and byte-for-byte continuity from each later fixture's previous fat pointer to the prior fixture's trailing fat pointer. It records that deserialization accepts any header count up to 2048 and therefore bypasses the sigma count required by `BftBlock::try_from`. |
| `SampleChangedAfter` / stale-stream checks | `zebra-crosslink/src/lib.rs::validate_bft_block_from_malachite_already_locked`; `proposal_status_against_current_stream` | During voting, the prototype recomputes the current candidate from the latest tip and returns `TMStatus::Stale` when the proposal candidate differs from the current `head - sigma` candidate. |
| `FinalizeCandidate` | Push/accept decided Crosslink BFT block | The model allows skipped PoW heights on the same branch. |
| `DecideAt` / `DecisionCursorIsSequential` | BFT-height progression and duplicate-decision rejection | The first multi-height model requires sequential BFT heights while permitting skipped PoW heights inside a valid candidate. |
| `height` / `HeightCursorSequential` / `FutureHeightsRemainPristine` | Validator-local BFT-height cursor and per-height round state | The heighted round-machine slice checks that future heights stay untouched until reached and that a validator cannot decide height `h + 1` before height `h`. |
| `FinalizeNextDecided` / `FinalityCursorSequential` | Finality application for locally decided BFT heights | The heighted-finality slice requires a local heighted decision before finality advances, and still rejects a decided PoW fork that does not extend the current finalized prefix. |
| `FinalizedPrefixLinear` | Crosslink finalized-prefix safety | Finalized snapshots must remain on one PoW branch. |

## Evidence and Accountability

| Quint predicate | Implementation counterpart | Notes |
| --- | --- | --- |
| `LocalPrecommitQuorum(p, r, v)` | Receiver-local Tenderlink precommit quorum | Used by the protocol state machine. |
| `LocalNilPrecommitCert(p, r)` | Receiver-local `2f + 1` nil-precommit quorum | Used to clear same-round state. |
| `PrecommitQuorum(r, v)` / `NilPrecommitCert(r)` | Broadcast-level quorum helpers | Used by safety witnesses and invariants. |
| `ObservedPrecommitQuorum(r, v)` | Observer/bookkeeping precommit evidence | Evidence for a fat pointer or precommit certificate for value `v`. |
| `ObservedNilPrecommitCert(r)` | Observer/bookkeeping nil-precommit evidence | Evidence for `2f + 1` signed nil precommits for round `r`. |
| `ObservedFatPointerQuorum(r, v)` | Decided/fat-pointer evidence | Signer set has quorum voting power and matching observed precommits. |
| `MessageEvidenceSoundness` | Protocol-message observer mirror | Invariant that live protocol messages are mirrored into observer evidence and fat-pointer evidence validates against observed signatures. |
| `CrosslinkEvidenceGossip.qnt` / `ObservedMessagesWereGossiped` | Evidence gossip and observer ingestion pipeline | Observer-local evidence is only accepted after the corresponding signed message or fat pointer appears in gossip. |
| `CrosslinkHeightedEvidenceGossip.qnt` / `ObservedMessagesWereGossiped` | Height-indexed evidence gossip and observer ingestion pipeline | Observer-local evidence is only accepted after same-height gossip, and heighted fat pointers require same-height observed signer precommits. |
| `CrosslinkMessageAuth.qnt` / `ProposalAuthentic` / `VoteAuthentic` | Signature verification and canonical sign bytes | Signed messages are accepted only when their bytes match the claimed proposal/vote/fat-pointer payload and verify for the claimed validator. |
| `CrosslinkHeightedMessageAuth.qnt` / `ProposalAuthentic` / `VoteAuthentic` | Height-indexed signature verification and canonical sign bytes | The heighted authentication slice binds BFT height into proposal, vote, and fat-pointer sign bytes, including a cross-height replay rejection witness. |
| `CrosslinkHeightedMessageGossipTransport.qnt` / `ProposalEnvelopeValid` / `PrevoteEnvelopeValid` / `PrecommitEnvelopeValid` / `FatPointerSigEnvelopeValid` | Future Crosslink consensus gossip envelope for signed Tenderlink messages | Inserts a general topic/kind envelope boundary before the heighted message-authentication slice. Proposals, prevotes, precommits, and fat-pointer signatures must arrive on the Crosslink consensus topic with the expected kind before canonical byte/signature checks accept them; votes reject non-nil `validRound` sidecars, and witnesses cover wrong topic, wrong kind, forged signature, wrong bytes, cross-height replay, and raw acceptance without a transport envelope. Envelope bytes remain compact model tokens rather than concrete production message vectors. |
| `CrosslinkTenderlinkVoteSignBytes.qnt` / `VoteSignWireCanonical` / `FatPointerPayloadCompatible` | `forks/tenderlink/src/lib.rs::make_vote_sign_datas`; `zebra-crosslink/src/malctx.rs::MalVote::to_bytes`; `forks/tenderlink/src/lib.rs::round_data_to_fat_pointer` | Pins the current concrete vote sign-byte layout: 32-byte validator public key, 32-byte value id or all zeroes for nil, little-endian u64 BFT height, and little-endian u32 round with bit 31 set for precommit. The witnesses pin exact nil prevote, value prevote, nil precommit, and value precommit hex strings; reject trailing bytes, wrong precommit flags, and cross-height replay bytes; and check that a value precommit's 44-byte value/height/round-type suffix is the fat-pointer payload after removing the signer pubkey. |
| `CrosslinkTenderlinkProposalChunkSignBytes.qnt` / `ProposalChunkWireCanonical` | `forks/tenderlink/src/lib.rs::PacketProposalChunkHeader::write_to`; `forks/tenderlink/src/lib.rs::TMMsgData::Proposal`; `forks/tenderlink/src/lib.rs::check_and_incorporate_msg` | Pins the legacy Tenderlink proposal chunk sign bytes: 56-byte little-endian chunk header followed by chunk data. The header layout is chunk index, proposal size, round, serialized valid round, height, and 32-byte proposal id. `valid_round = -1` serializes as `u32::MAX`; non-nil valid rounds must be less than the proposal round. Witnesses reject future valid rounds, trailing bytes, cross-height replay bytes, and chunks whose bounded chunk range exceeds the proposal size. |
| `CrosslinkTenderlinkVotePacketFormat.qnt` / `VotePacketCanonical` / `VotePacketEntryCanonical` | `forks/tenderlink/src/lib.rs::PacketVotes::write_to`; `forks/tenderlink/src/lib.rs::PacketVotes::read_from`; `forks/tenderlink/src/lib.rs::make_vote_sign_datas` | Pins the legacy Tenderlink vote-signature batch packet: one-byte nil/yes counts, u32 round, u64 height, 32-byte value id, then 66-byte roster-index/signature entries. The model records the `size_of::<PacketVotes>() == 1240` padding gap while `write_to` serializes only the used entries, and checks that receivers reconstruct canonical nil-prevote, nil-precommit, and value-precommit sign bytes from packet type, count position, roster pubkey, height, round, and value id, including three-signature all-nil, mixed nil/value, and all-value precommit quorum fixtures. |
| `CrosslinkTenderlinkProposalPolEvidence.qnt` / `AcceptProposalWithPol` | Proposal validation path that consumes decoded proposal chunks plus decoded prevote evidence | Requires non-nil valid-round proposals to have production-shaped POL evidence: canonical prevote packets, no nil votes in the bounded fixtures, sufficient packet-derived voting power, and the same BFT height, valid round, and value id as the proposal chunk. The fixtures now cover a two-signature weighted quorum, a three-signature f = 1 quorum, and a five-entry larger-validator-set quorum with a higher threshold, and reject forged certified-power sidecars. |
| `CrosslinkTenderlinkProposalPolTransport.qnt` / `AcceptTransportedProposalWithPol` | Known-peer receive path before decoded proposal/POL evidence is consumed | Links the decoded proposal/POL evidence rule to exact compact Tenderlink transport packets: the non-nil proposal chunk and the POL prevote packet must be decrypted, carry modeled nonce prefixes, use the expected packet type, and match exact packet-header plus payload bytes before transported POL evidence can be accepted. Witnesses reject missing proposal transport, missing POL transport, wrong packet type, wrong POL payload, wrong packet bytes, and low-power transported POL. |
| `CrosslinkTenderlinkConsensusPacketFormat.qnt` / `PacketHeaderCanonical` / `ConsensusPacketCanonical` | `forks/tenderlink/src/lib.rs::PacketHeader::{new_,write_to}`; `forks/tenderlink/src/lib.rs::PacketProposalChunkHeader::write_to`; `forks/tenderlink/src/lib.rs::PacketVotes::write_to`; Tenderlink proposal, prevote, and precommit send paths | Pins the legacy compact consensus packet envelope: a 16-byte little-endian `PacketHeader` where the low tag byte carries packet type/status, the high bytes carry `ack_latest`, and the second u64 carries `ack_field`, followed by either signed proposal-chunk bytes plus a 64-byte proposer signature or a compact `PacketVotes` payload. The witnesses include nonzero ack bytes, the non-nil POL proposal chunk packet, all-nil, mixed, and all-value precommit quorum packet envelopes, variable-length 112/178/244/376-byte vote-batch payloads, record the status flag at the header layer, and reject status-flag proposal packets, wrong packet types, malformed ack/tag fields, missing proposal signatures, wrong POL vote-batch kind, wrong weighted/large batch length, and trailing vote bytes. |
| `CrosslinkTenderlinkPrecommitTransport.qnt` / `ReceivePrecommitTransport` / `AcceptNilPrecommitCertificate` / `AcceptValuePrecommitCertificate`; `CrosslinkTenderlinkPrecommitTransportSafety.qnt` / `TransportSafety` | Known-peer receive path for compact precommit `PacketVotes` before nil-unlock or value-commit certificate consumption | Adds the decrypted compact transport gate for nil, value, and mixed precommit batches. A low-power precommit or mixed precommit quorum can be routed as a message, but certificate evidence is accepted only after the exact transported three-signature all-nil or all-value precommit quorum packet. Witnesses reject missing transport, low-power certificate attempts, mixed-quorum unlock or commit attempts, failed decrypts, prevote packets, wrong packet type, wrong payload, and wrong consensus-packet bytes. The direct safety slice preserves the same certificate boundary in an Apalache-friendly state machine. |
| `CrosslinkTenderlinkAccountabilityEvidenceFormat.qnt` / `AccountabilityEvidenceCanonical` / `NilValuePrecommitEquivocationEvidence` / `ValueValuePrecommitEquivocationEvidence` | Future slashing-evidence encoding for Tenderlink precommit equivocation | Pins concrete accountability envelopes for the nil-precommit rule and ordinary value/value equivocation: a typed 51-byte header identifies evidence kind, height, round, validator pubkey, packet types, and two packet lengths, followed by two canonical `PacketVotes` payloads. The accepted vectors are one validator signing nil plus value, or two distinct values, at the same height and round. Witnesses reject prevote evidence, wrong height/round, wrong length, same-value packets, wrong claimed signer, and wrong envelope bytes. |
| `CrosslinkTenderlinkAccountabilityEvidenceTransport.qnt` / `ReceiveAccountabilityEvidenceTransport` / `AcceptTransportedAccountabilityEvidence` | Gossip/receive path before decoded slashing evidence is consumed | Adds the Crosslink consensus-topic transport gate for the nil/value and value/value precommit equivocation envelopes. Transported evidence is accepted only after an `accountability-evidence` envelope carries exact canonical evidence bytes and matching height, round, and validator pubkey metadata. Witnesses reject missing transport, wrong topic, wrong kind, wrong length, wrong height, wrong signer, wrong bytes, and non-canonical evidence. |
| `CrosslinkTenderlinkAccountabilityObserver.qnt` / `ObservePrecommitEquivocation` | Observer path that turns accepted slashing evidence into an accountability fact | Bridges transported evidence into observer-local accountability facts. The observer records nil/value or value/value precommit equivocation only after the exact canonical evidence has already been accepted by the transport gate. Witnesses reject observation without transport, observation after receive-before-accept, and non-canonical evidence. |
| `CrosslinkTenderlinkAccountabilityObserverBridge.qnt` / `ApplyObservedEquivocationToAbstractEvidence` | Adapter from production-shaped observer facts into the abstract round-recovery accountability model | Maps concrete observer equivocations into `CrosslinkResampling`'s `evidencePrecommit` state. The bridge treats the Tenderlink validator pubkey as the abstract validator id, maps the concrete nil value id to `NilSnapshot`, proves value/value evidence reaches `CorrectSameRoundEquivocationEvidence`, and proves nil/value evidence stays signer-level until separate nil-certificate and value-quorum context is present. |
| `CrosslinkTenderlinkAccountabilityObserverBridgeSafety.qnt` / `BridgeSafety` | Verifier-friendly safety projection of the observer-to-abstract bridge | Keeps the same observer-to-abstract accountability obligations in a direct state machine for Apalache: applied observer facts must have matching abstract signer evidence, value/value evidence must reach same-round accountability, and nil/value evidence only reaches aggregate nil-unlock accountability with nil-certificate plus value-quorum context. |
| `CrosslinkTenderlinkUnlockAccountabilityBoundary.qnt` / `AbstractValueSwitchWithoutNilUnlock` / `BoundarySafety` | Future invalid-unlock evidence boundary | Records that a cross-round value switch is only an abstract bad-unlock signal while no nil-precommit certificate for the abandoned round is known. The witness suite rejects treating absence of a nil certificate as standalone slashing evidence, because a valid nil certificate can arrive later and justify the switch. Mixed precommits remain insufficient to clear the signal. |
| `CrosslinkTenderlinkNonceAckTransport.qnt` / `NonceOk` / `NonceUpdate` / `KnownPeerTransportPacketCanonical` | `forks/tenderlink/src/lib.rs::send_noise_msg`; `forks/tenderlink/src/lib.rs::nonce_is_ok`; `forks/tenderlink/src/lib.rs::nonce_update`; known-peer receive path before `read_header_and_maybe_status` | Models the known-peer stateless Noise transport boundary around compact consensus packets. The outer u64 nonce prefix is pinned as little-endian bytes, Noise authentication is represented by a `decrypted` guard, the decrypted plaintext must be a canonical compact consensus packet, and the nonce replay window mirrors the implementation's current/latest, 64-packet ack window, and 512-packet forward-jump tolerance. The witnesses include the implementation's existing `nonce_is_ok` examples, replay rejection, current-ack rejection, tolerance-boundary acceptance, beyond-tolerance rejection, bad nonce-prefix rejection, failed-decrypt rejection, and bad decrypted consensus packet rejection. |
| `CrosslinkTenderlinkStatusPacketFormat.qnt` / `StatusHeaderCanonical` / `StatusPacketCanonical` | `forks/tenderlink/src/lib.rs::PacketStatus::{write_to,read_from}`; `forks/tenderlink/src/lib.rs::write_header_and_maybe_status`; `forks/tenderlink/src/lib.rs::read_header_and_maybe_status` | Pins the inline status request body read after a status-flagged 16-byte `PacketHeader`: u64 height, u32 round, one proposal chunk range, one prevote range, and one precommit range. The witnesses pin exact status-only and acked-status hex vectors and reject missing status flags, reversed proposal ranges, vote ranges past the roster, malformed ack/tag fields, and truncated status packets. |
| `CrosslinkTenderlinkGossipRouter.qnt` / `RouterSafety` / `RouteProposalChunkTransport` / `RoutePrecommitTransport` / `RouteAccountabilityEvidenceTransport` / `RouteKnownPeerTransport` / `RouteStatusPacket`; `CrosslinkTenderlinkGossipRouterSafety.qnt` / `RouterSafety` / `RouterNext` | Future Tenderlink gossip router/registry integration across compact consensus, evidence, known-peer, and status lanes | Composes the current Tenderlink transport slices behind a shared channel registry. Proposal/POL packets, nil/value/mixed precommit packets, nil/value precommit certificates, accountability evidence, known-peer consensus packets, and status packets all stay on the Crosslink consensus topic, but accepted messages are routed through separate channel/kind namespaces while preserving each underlying transport safety invariant. Mixed precommit quorums route as wait/message records, not nil-unlock or value-commit certificates. The direct safety slice rejects wrong topics, wrong kinds, wrong bytes, cross-channel records, and unknown-channel records in an Apalache-friendly state machine. |
| `CrosslinkMalachiteProposalProtobufFormat.qnt` / `ProposalProtoCanonical` / `SignedProposalProtoCanonical` / `StreamedProposalProtoCanonical` | `zebra-crosslink/proto/consensus.proto`; `zebra-crosslink/src/malctx.rs::MalValue::{to_proto,from_proto}`; `zebra-crosslink/src/malctx.rs::MalProposal::{to_proto,from_proto}`; `zebra-crosslink/src/malctx.rs::MalStreamedProposal::{to_proto,from_proto}`; `zebra-crosslink/src/malctx.rs::MalProtobufCodec` | Pins small exact prost/proto3 vectors for Malachite proposal bytes: `Value`, `Proposal` with and without `pol_round`, `SignedMessage::Proposal`, and streamed proposal parts. The witnesses record the protobuf tags and lengths, the omitted nil POL round, the required 32-byte validator/proposer address, the required 64-byte signature, and rejection of missing values, nil proposal rounds, short validator/proposer keys, wrong oneof tags, and missing signatures. |
| `CrosslinkMalachiteProposalGossipTransport.qnt` / `ProposalEnvelopeValid` / `SignedProposalEnvelopeValid` / `StreamedProposalEnvelopeValid` / `TransportSafety` | `zebra-crosslink/proto/consensus.proto`; `zebra-crosslink/src/malctx.rs::MalProtobufCodec`; future Malachite proposal gossip transport integration | Adds a Crosslink proposal-topic envelope boundary before the exact proposal protobuf decoder model. The witnesses require the right topic, raw/signed/streamed envelope kind, and exact canonical bytes before accepting proposal messages, and reject raw signed/streamed decoder acceptance without matching transport envelopes. |
| `CrosslinkMalachiteLivenessProtobufFormat.qnt` / `PolkaCertificateProtoCanonical` / `RoundCertificateProtoCanonical` / `LivenessRoundMessageProtoCanonical` | `zebra-crosslink/proto/liveness.proto`; `zebra-crosslink/src/malctx.rs::mal_encode_polka_certificate`; `zebra-crosslink/src/malctx.rs::mal_decode_polka_certificate`; `zebra-crosslink/src/malctx.rs::mal_encode_round_certificate`; `zebra-crosslink/src/malctx.rs::mal_decode_round_certificate`; `zebra-crosslink/src/malctx.rs::MalProtobufCodec` | Pins exact prost/proto3 vectors for a polka certificate, a skip-round nil certificate, and a value precommit certificate under their `LivenessMessage` oneof wrappers. The witnesses record enum default omission for precommit certificates, non-default skip/precommit vote-type encoding, omitted nil value ids, included value ids, required 32-byte validator addresses, required 64-byte signatures, and wrong-oneof rejection. |
| `CrosslinkMalachiteLivenessGossipTransport.qnt` / `PolkaEnvelopeValid` / `RoundEnvelopeValid` / `TransportSafety` | `zebra-crosslink/proto/liveness.proto`; `zebra-crosslink/src/malctx.rs::MalProtobufCodec`; future Malachite liveness gossip transport integration | Adds a Crosslink liveness-topic envelope boundary before the exact polka/round certificate protobuf decoder model. The witnesses require the right topic, polka/round envelope kind, and exact canonical bytes before accepting liveness messages, and reject raw decoder acceptance, wrong topics/kinds/bytes, wrong oneof tags, missing polka values, precommit-nil round certificates, and skip-value round certificates. |
| `CrosslinkMalachiteSyncProtobufFormat.qnt` / `SyncRequestProtoCanonical` / `SyncResponseProtoCanonical` / `CommitCertificateProtoCanonical` | `zebra-crosslink/proto/sync.proto`; `zebra-crosslink/src/malctx.rs::mal_encode_sync_response`; `zebra-crosslink/src/malctx.rs::mal_decode_sync_response`; `zebra-crosslink/src/malctx.rs::mal_encode_synced_value`; `zebra-crosslink/src/malctx.rs::mal_decode_synced_value`; `zebra-crosslink/src/malctx.rs::mal_encode_commit_certificate`; `zebra-crosslink/src/malctx.rs::mal_decode_commit_certificate`; `zebra-crosslink/src/malctx.rs::MalProtobufCodec` | Pins exact prost/proto3 vectors for `ValueRequest`/`SyncRequest`, `CommitCertificate`, `SyncedValue`, and `ValueResponse`/`SyncResponse` with and without an attached value. The witnesses record the same raw bytes ambiguity between `SyncRequest` and no-value `SyncResponse` in distinct message contexts, and reject bad heights, wrong oneof tags, missing value ids/certificates, and short signer addresses. |
| `CrosslinkMalachiteSyncGossipTransport.qnt` / `SyncRequestEnvelopeValid` / `SyncResponseEnvelopeValid` / `TransportSafety` | `zebra-crosslink/proto/sync.proto`; `zebra-crosslink/src/malctx.rs::MalProtobufCodec`; future Malachite sync gossip transport integration | Adds a Crosslink sync-topic envelope boundary before the exact `SyncRequest`/`SyncResponse` protobuf decoder model. The witnesses require the right topic, request/response envelope kind, and exact canonical bytes, preserve kind separation when a `SyncRequest` and no-value `SyncResponse` share raw bytes, and reject raw decoder acceptance without a matching transport envelope. |
| `CrosslinkMalachiteGossipRouter.qnt` / `RouterSafety` / `RouteProposal` / `RoutePolka` / `RouteSyncRequest` | Future Malachite gossip router/registry integration across proposal, liveness, and sync channels | Composes the three Malachite transport machines behind a shared router. Each accepted proposal/liveness/sync envelope is also recorded on the matching router channel, the router checks disjoint topics and channels, and wrong-channel raw injection violates safety. This is a channel registry and composition boundary; the per-channel transport specs still own exact protobuf payload canonicality. |
| `CrosslinkMalachiteGossipRouterSafety.qnt` / `RouterSafety` / `RouterNext` | Future Malachite gossip router/registry integration across proposal, liveness, and sync channels | Provides the verifier-friendly direct state-machine version of the same channel/topic/kind registry contract, because the alias-imported transport composition is executable under the Rust backend but currently too hard for the Apalache flattener. |
| `CrosslinkProductionGossipRegistry.qnt` / `RegistrySafety` / `RouteProduction` | Future production gossip registry spanning Tenderlink compact consensus lanes, Malachite protobuf lanes, dynamic-sigma consensus-param lanes, and production-finality proposal/fat-pointer lanes | Adds a direct production-level registry above all four router families. The slice checks that Tenderlink consensus/evidence/known-peer/status records, Malachite proposal/liveness/sync records, dynamic-sigma consensus-param records, and production-finality proposal/fat-pointer records stay separated by protocol, topic, channel, kind, and byte label, including rejection witnesses for Malachite proposal traffic on the Tenderlink consensus channel, Tenderlink mixed-precommit traffic on the Malachite liveness channel, consensus-param traffic on Tenderlink/Malachite lanes, and finality proposal/fat-pointer traffic on unrelated lanes. The dynamic-sigma lane uses exact raised/recovered production-shaped consensus-param hex vectors pinned by the format model; the finality lanes use compact byte labels whose route validity directly checks the generated fixture version/height/finality/offset/length/header-count metadata also checked by `CrosslinkProductionFinalityProjectionContract.qnt`. |
| `CrosslinkProductionGossipIngress.qnt` / `IngressSafety` / `Accept*` | Future production node gossip ingress before downstream Tenderlink, Malachite, dynamic-sigma, and production-finality handlers | Adds a node-local ingress gate above the production registry. Accepted envelopes must be registry-valid for the exact downstream lane they enter, so registry-valid evidence cannot be handed to the Tenderlink consensus lane, Malachite proposal traffic cannot enter Tenderlink ingress, dynamic-sigma consensus params cannot enter Tenderlink/Malachite ingress, production-finality proposal/fat-pointer records cannot enter unrelated ingress lanes, and cross-protocol raw injections violate ingress safety. |
| `CrosslinkProductionFinalityIngressBridge.qnt` / `FinalityIngressBridgeSafety` / `EventuallyFinalityIngressBridgeFinalizes` | Future production finality node ingress feeding proposal/evidence projection and finality application | Bridges the node-local production-finality ingress gate into the production finality projection boundary. The checked-in production BFT-block proposal and fat-pointer wire can become projection-ready only after their matching production-finality ingress records are accepted, finality waits on both projected records, and Tenderlink precommit traffic cannot satisfy the production BFT-block proposal ingress lane. |
| `CrosslinkProductionFinalityIngressProjectionBridge.qnt` / `IngressProjectionBridgeSafety` / `EventuallyIngressProjectionFinalizes` | Future production finality node ingress feeding the concrete projection action graph | Composes `CrosslinkProductionGossipIngress.qnt` with `CrosslinkProductionFinalityProjectionContract.qnt` directly. The Rust-backed tests run the happy path through production ingress, proposal transport, candidate observation, precommit/fat-pointer evidence, and finality, while witnesses show that raw projection without ingress violates the bridge safety predicate and that Tenderlink precommit traffic cannot satisfy the production proposal lane. This is intentionally a typecheck/test-only imported-action bridge for now; Apalache/TLC proof coverage stays on the scalar `CrosslinkProductionFinalityIngressBridge.qnt` until the nested imported graph can be flattened cleanly. |
| `CrosslinkProductionFinalityIngressProjectionBridgeSafety.qnt` / `Safety` / `EventuallyProductionIngressProjectionFinalizes` | Future production finality node ingress feeding staged projection and finality application | Provides the verifier-friendly direct state-machine projection for the imported ingress/projection bridge. It proves that production proposal transport requires production proposal ingress, candidate observation requires proposal transport, fat-pointer transport requires fat-pointer ingress, fat-pointer observation requires both transport and precommit evidence, finality requires the complete projected path, and Tenderlink precommit ingress alone cannot advance the production proposal path. |
| `CrosslinkHeightedValidatorEvidence.qnt` / `AuthorizedAtHeight` | Validator-set-aware evidence validation | Observed precommits and fat pointers are checked against the validator set active at the evidence BFT height, so removed validators remain valid for old-height evidence but cannot sign next-height evidence after rotation. |
| `CrosslinkHeightedAuthenticatedEvidence.qnt` / `PrecommitAuthentic` / `ObservedFatPointerValid` | Authenticated evidence pipeline | Precommit evidence must pass canonical sign-byte verification before gossip and observation, observer evidence must come from prior gossip, and fat pointers require quorum signer precommits plus authenticated fat-pointer signatures from validators authorized at that BFT height. |
| `CrosslinkHeightedAuthenticatedGossipTransport.qnt` / `PrecommitTransportEnvelopeValid` / `FatPointerSigTransportEnvelopeValid` / `TransportSafety` | Future Crosslink consensus gossip envelope for Tenderlink precommits and fat-pointer signatures | Inserts a general transport boundary before heighted authenticated evidence. A precommit or fat-pointer signature must appear in a Crosslink-topic envelope with the expected kind, canonical signed bytes, validator signature, BFT height, round, and active validator authorization before it can be gossiped or observed; wrong topic/kind/bytes, cross-height replay, and raw gossip without a transport envelope are rejected. Envelope bytes are still compact Quint tokens rather than concrete production Tenderlink message vectors. |
| `CrosslinkFatPointerFormat.qnt` / `FatPointerFormatValid` / `FatPointerWireEnvelopeValid` / `FatPointerDerivedFromRoundData` / `SignaturePubKeysUnique` / `SignatureAuthorizedAtHeight` | `zebra-crosslink/src/lib.rs::FatPointerToBftBlock2`, `FatPointerSignature2`, `to_bytes`, `validate_signatures`; `zebra-chain/src/block/header.rs::FatPointerToBftBlock`, `FatPointerSignature`, `to_bytes`, `from_bytes`, `zcash_deserialize`; `forks/tenderlink/src/lib.rs::FatPointerToBftBlock3`, `FatPointerSignature3`, `round_data_to_fat_pointer`, `make_vote_sign_datas`, `TMSig::verify` | The model mirrors the current production count-plus-vector format: a 44-byte vote payload suffix without the finalizer pubkey, little-endian u16 signature count at byte offset 44, entries starting at byte offset 46, and one 32-byte pubkey plus 64-byte vote signature per entry. It makes exact wire-envelope offsets and length, duplicate-pubkey rejection, height-scoped signer authorization, canonical signed bytes, signature verification, quorum power, and derivation from producer round data explicit. |
| `CrosslinkFatPointerProductionVectors.qnt` / `SerializerCountSliceIsBytes44To46` / `StreamingDeserializerMatchesSerializer` / `PrototypeTryFromBytesGapRecorded` | `zebra-chain/src/block/header.rs::FatPointerToBftBlock::{to_bytes, zcash_deserialize, try_from_bytes}`; `zebra-crosslink/src/{lib.rs,malctx.rs}::FatPointerToBftBlock2::try_from_bytes`; `forks/tenderlink/src/lib.rs::FatPointerToBftBlock3::try_from_bytes`; `crosslink-test-data/test_pos_block_*.bin`; `fixtures/production-bft-block-vectors.json`; `spec/CrosslinkProductionFixtureVectorsGenerated.qnt` | Pins exact implementation-linked vectors for 0-4 signatures: 46, 142, 238, 334, and 430 bytes. The streaming serializer/deserializer agree on count bytes 44..46. The generated fixture constants anchor a previous fat pointer at byte 8 and a trailing fat pointer at byte 4619, each with one signer, count bytes at offsets 52 and 4663, signer pubkeys at 54 and 4665, and vote signatures at 86 and 4697. The current prototype helper `try_from_bytes` uses a reversed count slice (`bytes[44..2]`) in multiple copies; the spec records that as an implementation gap rather than treating it as the protocol layout. |
| `CrosslinkFatPointerAuthenticatedEvidence.qnt` / `ProductionFatPointerEvidenceValid` / `ProductionFatPointerWireEvidenceValid` / `GossipedMatchingPrecommitForSignature` | `forks/tenderlink/src/lib.rs::round_data_to_fat_pointer`; `forks/tenderlink/src/lib.rs::check_and_incorporate_msg`; `zebra-crosslink/src/lib.rs::new_decided_bft_block_from_malachite`; `zebra-crosslink/src/lib.rs::fat_pointer_has_roster_quorum` | Connects the production-shaped fat-pointer vector and counted wire envelope to observer evidence. Every active signer entry must have a matching gossiped precommit at the same height, round, and value before the fat pointer wire is observed; duplicate signer entries, trailing-byte wires, and removed-validator next-height precommits are rejected at the composed boundary. |
| `CrosslinkFixtureAuthenticatedEvidence.qnt` / `GeneratedFixtureWireConstantsCoherent` / `FixtureSafety` | `crosslink-test-data/test_pos_block_*.bin`; `fixtures/production-bft-block-vectors.json`; `spec/CrosslinkProductionFixtureVectorsGenerated.qnt`; `scripts/extract-bft-block-vectors.mjs`; `CrosslinkFatPointerAuthenticatedEvidence.qnt` | Bridges the generated one-signature production fixture constants into the authenticated observer pipeline. The fixture wire length is derived from the checked-in `BftBlockAndFatPointerToIt` envelope, and the model accepts the wire only after the matching abstract precommit has been gossiped. The generated artifacts pin the real payload, pubkey, vote signature, and `pubkey || payload` sign-data hex strings; the manifest validator verifies those Ed25519 signatures with Node crypto while the Quint model keeps crypto abstract. |
| `CrosslinkFixtureGossipTransport.qnt` / `FixturePrecommitTransportEnvelopeValid` / `FixtureFatPointerWireTransportEnvelopeValid` / `TransportSafety` | `crosslink-test-data/test_pos_block_*.bin`; `fixtures/production-bft-block-vectors.json`; `spec/CrosslinkProductionFixtureVectorsGenerated.qnt`; `scripts/extract-bft-block-vectors.mjs`; `CrosslinkFixtureAuthenticatedEvidence.qnt`; future Crosslink gossip transport implementation | Inserts a fixture-level transport boundary before observer acceptance. The generated fixture precommit and fat-pointer wire must be gossiped in canonical Crosslink-topic envelopes before the observer accepts the wire; wrong topic, wrong sign bytes, wrong kind, and wrong wire length are rejected. The invariant also checks that a fixture precommit cannot enter gossip unless its matching transport envelope was seen. The model keeps envelope bytes as compact tokens while the generated fixture manifest and script pin and verify the real byte strings. |
| `CrosslinkProductionFinalityProjectionContract.qnt` / `ProductionFinalitySafety` / `EventuallyProductionFixtureFinalized` | `CrosslinkBftBlockProductionVectors.qnt`; `CrosslinkFixtureGossipTransport.qnt`; `CrosslinkProductionFixtureVectorsGenerated.qnt`; future Crosslink proposal gossip and finality application | Composes the generated production BFT-block fixture, a production-shaped proposal transport envelope, fixture transport boundary, and authenticated fat-pointer observer evidence at the finality boundary. The composed model rejects BFT-block candidate observation before proposal transport, rejects wrong-topic/wrong-kind/wrong-length proposal envelopes, rejects envelopes whose serialized BFT-block version/height/finalization/header-prefix fields do not match the generated fixture, and rejects finality unless the transported checked-in BFT block candidate and transported fat-pointer wire have both been observed. A scalar TLC projection checks the same gates as a complete finite temporal graph so production-shaped finality progress remains in CI even while the alias-composed transport model stays safety-only. |
| `CorrectNilValueEquivocationEvidence` | Nil/value precommit evidence | Same validator signs both nil and value precommit in the same round. |
| `CorrectValueSwitchWithoutUnlock` | Conflicting value-switch evidence | Validator signs a later conflicting value without a nil certificate for the earlier lock round. |

## Gaps To Close

- Connect the fat-pointer format and observer models to more production
  serialization test vectors and the full Tenderlink validator-set rotation
  path.
- Turn the BFT-block validation-gap witnesses into production constructor
  coverage for version, header-order, and PoW-solution checks.
- Connect the abstract message-authentication, evidence, and standalone gossip
  models to concrete serialized message bytes and full production gossip. The
  heighted message and authenticated-evidence transport bridges now cover the
  topic/kind/auth envelope boundaries, and the Tenderlink vote sign-byte,
  legacy proposal-chunk sign-byte, vote packet, and compact consensus packet
  layouts are pinned. The known-peer nonce/ack transport replay boundary and
  inline status packet format are now modeled. Initial Malachite proposal,
  liveness, and sync protobuf vectors are pinned, first Malachite
  proposal/liveness/sync gossip-envelope bridges exist, and a shared
  Tenderlink router composition now separates the compact consensus, evidence,
  known-peer, and status namespaces, and a shared Malachite
  proposal/liveness/sync gossip router plus verifier-friendly router safety
  slice now compose those channel namespaces; the production registry, ingress,
  and finality-ingress bridge slices also cover dynamic-sigma lane separation
  and the production-finality path from ingress to projection readiness.
  Broader concrete node gossip integration remains open.
- Connect the dynamic-sigma consensus-param format/transport and production
  registry/ingress models to real `bc_confirmation_depth_sigma` implementation
  byte vectors, signatures, gossip, and node configuration update paths once
  the production format exists.
- Connect the height-indexed authentication and evidence-gossip slices to
  concrete serialized message bytes and full production gossip beyond the
  current compact-token transport bridge.
- Lift the current TLC-friendly progress contracts and imported-predicate
  bridge into temporal liveness checks over the full imported protocol action
  graph.
- Replace the current PoW stochastic-assumption numerators with production
  measured distributions for normal block arrivals, propagation races, GST
  scaling, and long-tail reorg depth. The raw PoW fixture interval contract now
  exists, but those fixtures are deterministic serialization samples rather
  than a production stochastic calibration set.
- Add more implementation-linked test vectors once the Tenderlink message
  format, Crosslink BFT block encoding, and PoW head/candidate sampling data
  stabilize; in particular, pin whether candidate validation should use the
  declared `head - sigma` height/hash or a header-vector position.
